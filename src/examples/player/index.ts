import { runInstanceChecks } from "./instance"
import { runManagerChecks } from "./manager"

export function runExamples(): void {
    print('Player examples')
    // runInstanceChecks()
    runManagerChecks()
}
